const { Embed, Discord } = require('discord.js');
const config = require('../../config.json');

exports.run = async (client, message, args) => {

}

exports.help = {
    name: "",
    description: "",
    usage: [`${config.prefix}`],
    example: [`${config.prefix}`]
}
  
  exports.conf = {
    aliases: [],
    cooldown: 1
}
